class Solution {
  public:
    string reverseString(string& s) {
        // Your code goes here
        
         int i=0; 
         int e=s.size()-1;
         

        while(i<=e){
         swap(s[i],s[e]);
          i++;
          e--;
    }
      return s;
    }
};
